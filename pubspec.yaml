name: app_02
description: "A new Flutter project."
publish_to: 'none' # Remova esta linha se desejar publicar no pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0' # Atualizado para uma versão mais recente

dependencies:
  flutter:
    sdk: flutter
  simple_animations: ^4.2.0 
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
